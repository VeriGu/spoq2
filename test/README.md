This directory contains integration tests that test spoq3 with (1) spec-gen stage and (2) automated invariant verification configuration files and source codes as testcases.

```
rcsm/:
	- rmm-opt.linked.ir2json.json: IR for rcsm-rmm system (json format)
	- **.v: configuration files
```
